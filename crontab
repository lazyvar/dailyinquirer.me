# Send the daily writing prompt. Runs hourly; the command decides who is
# due (local time at or past the user's chosen send hour, not yet sent today).
0 * * * * cd /app && python manage.py send_daily_mail
